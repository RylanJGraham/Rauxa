import { registerRootComponent } from 'expo'; // For Expo projects
import App from './App'; // Path to your main app component

// Register the main entry point of your app
registerRootComponent(App);
